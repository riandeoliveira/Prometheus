@use "../../styles" as *;

.title {
  font-size: 32px;
  font-weight: 600;
  text-align: center;
}

.subtitle {
  font-size: 24px;
  font-weight: 400;
  text-align: center;
}

.button {
  @include animated-button-component;

  background-color: $blue;
  border-radius: 6px;
  color: $white;
  cursor: pointer;
  font-weight: 500;
  padding: 8px;
}
